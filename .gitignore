.env
.claude/settings.local.json

# Dependencies
node_modules/

# Build output
out/
dist/
dist-electron/

# Local database files
*.db
*.db-journal
*.db-wal
*.db-shm
.drizzle-dev.db

# Test output
coverage/
playwright-report/
test-results/
.playwright-mcp/
# The screenshot sweep's output (`npm run screens`). Deliberately NOT under
# test-results/, which Playwright wipes at the start of every run — see the
# OUT constant in e2e/screenshots/sweep.sweep.ts. ~110 PNGs plus an
# index.html contact sheet: a review instrument, regenerated on demand.
screens/

# Logs / caches
*.log*
.eslintcache
.DS_Store
*.tsbuildinfo
